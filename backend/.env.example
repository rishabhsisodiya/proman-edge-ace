# Our own application DB (Postgres) — Prisma
DATABASE_URL="postgresql://user:password@localhost:5432/proman_ace?schema=public"

# Auth
JWT_SECRET=change-me
PORT=4100
FRONTEND_URL=http://localhost:3000

# Redis (BullMQ)
REDIS_URL=redis://localhost:6379

# --- ERPNext: read-only nightly master-data pull (direct MariaDB — reads only, never writes) ---
# Test server credentials (from /Users/rishabhsisodiya/tcb/PROMAN/backend/.env) — swap for
# production pispl.frappe.cloud / ace.frappe.cloud credentials once issued.
# OPEN ITEM: confirm with Shivam whether pispl or ace actually holds ACE-relevant
# Serial No / Item Price data — tabSerial No is empty and tabItem Price near-empty
# on this test DB as of this writing.
ERP_DB_HOST=187.127.182.29
ERP_DB_PORT=3306
ERP_DB_NAME=_61377ae1ad149e95
ERP_DB_USER=erpnext_remote
ERP_DB_PASSWORD=change-me
ERP_DB_SSL=false

# --- ERPNext: REST API for writes (Stock Entry, draft Sales Invoice) ---
# Integration user credentials — request from Shivam (see docs/erp-request-shivam.md)
ERPNEXT_BASE_URL=http://187.127.182.29:8000
ERPNEXT_API_KEY=
ERPNEXT_API_SECRET=

# Inbound webhook shared secret (ERPNext -> this app, on Sales Invoice submit)
ERPNEXT_WEBHOOK_SECRET=change-me

# Notification gateway (Sprint 2)
SENDGRID_API_KEY=
